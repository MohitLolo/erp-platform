# deploy/configmap/test/seata.yml
# K8s ConfigMap: erp-config-seata (test namespace)
# 挂载路径: /config/seata.yml
seata:
  enabled: true
  registry:
    type: file
    file:
      name: registry.conf
  config:
    type: file
